import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  profileNavigate: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
  profileNavigate__title: {
    marginBottom: 5,
  },
  profileNavigate__desc: {
    textAlign: "center",
  },
});
